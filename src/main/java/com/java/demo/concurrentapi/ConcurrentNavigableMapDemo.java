package com.java.demo.concurrentapi;

public class ConcurrentNavigableMapDemo {
	/*ConcurrentNavigableMap interface in Java is a ConcurrentMap supporting
	 *  NavigableMap operations, and recursively so for its navigable sub-maps. 
	 *  It was added in Java 1.6.

	Where NavigableMap is a SortedMap extended with navigation methods 
	returning the closest matches for given search targets. Methods 
	lowerEntry, floorEntry, ceilingEntry, and higherEntry return
	 Map.Entry objects associated with keys respectively less than,
	  less than or equal, greater than or equal, and greater than a given key, 
	  returning null if there is no such key. Similarly, methods lowerKey,
	   floorKey, ceilingKey, and higherKey return only the associated keys. 
	   All of these methods are designed for locating, not traversing entries.*/
	public static void main(String[] args) {

	}

}
